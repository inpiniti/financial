/**
 * 03. 모니시 파브라이 — 단도 투자 (앞면이면 크게 벌고, 뒷면이어도 별로 안 잃는다)
 * "내가 틀렸을 때 얼마나 잃는가?"
 * ※ _공통.js를 먼저 붙여넣을 것
 */
await tossScreen("03 모니시 파브라이", [
  F("시가총액", 1000 * 억, null),                                        // 1,000억원 이상
  F("PER", 0, 10),                                                       // 0~10배
  F("PBR", 0, 1),                                                        // 0~1배 (청산가치 방어막)
  FP("부채_비율", 기간_분기TTM("TTM"), null, 1, { includeTo: true }),     // 100% 이하
  FP("이자_보상_배율", 기간_분기TTM("TTM"), 3, null),                     // 300% 이상 (협상 불가)
  FP("ROE", 기간_분기TTM("TTM"), 0.05, null),                            // 5% 이상 (이미 굴러가는 사업)
]);

// 선택 추가: 순이익 연속증가 2년↑(밸류트랩 배제) / 유동비율 100%↑ / 배당수익률 3%↑
// 40개 초과 → PBR 0.7배 또는 ROE 10%로 조여라
// 20개 미만 → 이자보상배율 제거 후 시가총액 하한을 낮춰라
