/**
 * 04. 조엘 그린블라트 — 마법공식 근사 (필터 단 5개)
 * EY(이익수익률) ≈ EV/EBITDA, ROC(자본수익률) ≈ ROA + 영업이익률
 * "좋은 기업을 싼값에. 나머지 44개 필터는 잡음이다."
 * ※ _공통.js를 먼저 붙여넣을 것
 */
await tossScreen("04 조엘 그린블라트", [
  F("시가총액", 3000 * 억, null),                                        // 3,000억원 이상
  FP("EV_EBITDA", 기간_분기TTM("TTM"), 0, 10),                           // 0~10배 = 이익수익률(EY) 근사
  FP("ROA", 기간_분기TTM("TTM"), 0.1, null),                             // 10% 이상 = 자본수익률(ROC) 근사
  FP("영업_이익률", 기간_분기TTM("TTM"), 0.1, null),                      // 10% 이상 (본업 이익 확인)
]);

// ⚠️ ROE 대신 ROA를 쓰는 이유: ROE는 부채로 쉽게 부풀릴 수 있다
// ⚠️ 스크리너는 두 지표의 "순위 합산"을 못 한다.
//    뽑힌 20~40개를 엑셀에 옮겨 EV/EBITDA 순위 + ROA 순위를 직접 더해야 진짜 마법공식이다
// 40개 초과 → ROA·영업이익률 건드리지 말고 EV/EBITDA를 8배로 먼저 조여라
// 20개 미만 → 시가총액 하한을 1,000억으로 낮춰라
