/**
 * 00. 13인 공통분모 세트 — 어느 스타일로 가든 이건 깔고 시작하라
 * 근거: docs/tossfilter.md 3절
 *   부채비율(13/13) · 이자보상배율(12/13) · 시가총액 하한(12/13) · 이익의 질(10/13)
 * ※ _공통.js를 먼저 붙여넣을 것
 */
await tossScreen("00 공통분모 (13인 합의)", [
  F("시가총액", 3000 * 억, null),              // 3,000억원 이상
  FP("부채_비율", 기간_분기TTM("TTM"), null, 1, { includeTo: true }),   // 100% 이하
  FP("이자_보상_배율", 기간_분기TTM("TTM"), 3, null),                    // 300% 이상 (좀비기업 배제)
  FP("영업_이익률", 기간_분기TTM("TTM"), 0.1, null),                     // 10% 이상
  FP("ROE", 기간_분기TTM("TTM"), 0.1, null),                            // 10% 이상
]);

// 결과가 40개 초과 → ROE 0.15, 영업이익률 0.15로 상향
// 결과가 20개 미만 → 이자보상배율 제거 또는 시가총액 1,000억으로 완화
// 부채비율은 절대 먼저 풀지 말 것 (13인 최다 합의 항목)
