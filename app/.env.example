# Supabase (guru_votes 읽기 전용). 이 파일을 app/.env로 복사한 뒤 실제 값을 넣는다.
# 값은 저장소 루트 .env의 SUPABASE_URL / SUPABASE_ANON_KEY와 같다.
# 배포(Vercel)에서는 프로젝트 환경변수에 같은 이름으로 등록한다.
VITE_SUPABASE_URL=https://xxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=publishable_or_anon_key
